{
  "name": "menu-kalaghar",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run --reporter=dot",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vite": "^7.1.0",
    "vitest": "^2.0.5"
  }
}
